# Phase 1: Core Above-the-Fold Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Scaffold the Next.js project and build the core above-the-fold experience: Navbar, Hero, Quiz/Guide section, and Footer.

**Architecture:** Single-page Next.js 14+ App Router landing page. All sections are client components using Framer Motion for animations. Tailwind CSS handles all styling with brand tokens defined in the config. Content lives in `lib/constants.ts` so copy can be updated without touching components.

**Tech Stack:** Next.js 14+, Tailwind CSS 3.x, Framer Motion, TypeScript, next/font/google

---

## File Structure

```
j.burns-dream-smile/
├── app/
│   ├── layout.tsx              # Root layout: fonts, metadata, body classes
│   ├── page.tsx                # Landing page assembling all sections
│   └── globals.css             # Tailwind directives + CSS custom properties
├── components/
│   ├── Navbar.tsx              # Sticky navbar with scroll behavior
│   ├── Footer.tsx              # Dark footer with contact info
│   ├── ui/
│   │   ├── Button.tsx          # Reusable pill button (solid/outlined)
│   │   ├── SectionLabel.tsx    # Small uppercase teal label
│   │   └── CheckItem.tsx       # Teal checkmark + text row
│   └── sections/
│       ├── HeroSection.tsx     # Dark overlay hero with CTA
│       └── QuizAndGuideSection.tsx  # 3-column quiz + book + guide
├── lib/
│   └── constants.ts            # All site copy and content data
├── public/
│   └── images/                 # Empty dirs for future assets
├── tailwind.config.ts          # Brand tokens, fonts, colors
├── next.config.js              # Image formats config
├── tsconfig.json               # (generated by create-next-app)
└── package.json
```

---

### Task 1: Scaffold Next.js Project

**Files:**
- Create: entire project via `create-next-app`
- Modify: `package.json` (add framer-motion)

- [ ] **Step 1: Create Next.js project**

Run from the project directory (which already has `claude.md`):
```bash
cd "/c/Users/Franz Samilo/Desktop/OZ Projects/j.burns-dream-smile"
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --use-npm
```

When prompted about overwriting, select yes. This scaffolds into the current directory.

Expected: Project scaffolded with `app/`, `public/`, `tailwind.config.ts`, etc.

- [ ] **Step 2: Install Framer Motion**

```bash
npm install framer-motion
```

Expected: `framer-motion` added to `package.json` dependencies.

- [ ] **Step 3: Create image directories**

```bash
mkdir -p public/images/{hero,patients,benefits,dr-burns,services,guide,backgrounds,icons,logos}
```

- [ ] **Step 4: Verify dev server starts**

```bash
npm run dev
```

Expected: Server starts on localhost:3000 with no errors.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: scaffold Next.js 14 project with Tailwind and Framer Motion"
```

---

### Task 2: Configure Tailwind, Fonts, and Global Styles

**Files:**
- Modify: `tailwind.config.ts`
- Modify: `app/layout.tsx`
- Modify: `app/globals.css`
- Modify: `next.config.js`

- [ ] **Step 1: Update tailwind.config.ts with brand tokens**

Replace the entire contents of `tailwind.config.ts`:

```ts
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./app/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        teal: {
          DEFAULT: "#7FB5B0",
          dark: "#5A9A93",
          light: "#A8D5D0",
        },
        charcoal: "#3A3A3A",
        "dark-slate": "#4A4A4A",
        "warm-gray": "#6B6B6B",
        coral: "#E88B7A",
        "off-white": "#FAFAF8",
        "red-muted": "#D9534F",
        "gold-star": "#F5A623",
      },
      fontFamily: {
        display: ["var(--font-playfair)", "serif"],
        body: ["var(--font-montserrat)", "sans-serif"],
        script: ["var(--font-dancing-script)", "cursive"],
      },
      maxWidth: {
        content: "1200px",
      },
      borderRadius: {
        pill: "9999px",
      },
    },
  },
  plugins: [],
};

export default config;
```

- [ ] **Step 2: Update app/layout.tsx with fonts and metadata**

Replace the entire contents of `app/layout.tsx`:

```tsx
import type { Metadata } from "next";
import { Playfair_Display, Montserrat, Dancing_Script } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

const montserrat = Montserrat({
  subsets: ["latin"],
  variable: "--font-montserrat",
  display: "swap",
});

const dancingScript = Dancing_Script({
  subsets: ["latin"],
  variable: "--font-dancing-script",
  display: "swap",
});

export const metadata: Metadata = {
  title: "DreamSmile by Dr. Jeffrey S. Burns, DDS — Dental Implants",
  description:
    "A Dream Smile You Never Have to Hide. 30+ years of dental implant experience. Schedule your free consultation with Dr. Burns today.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${playfair.variable} ${montserrat.variable} ${dancingScript.variable}`}
    >
      <body className="font-body text-dark-slate bg-off-white antialiased">
        {children}
      </body>
    </html>
  );
}
```

- [ ] **Step 3: Update app/globals.css**

Replace the entire contents of `app/globals.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --color-teal: #7FB5B0;
  --color-teal-dark: #5A9A93;
  --color-teal-light: #A8D5D0;
  --color-charcoal: #3A3A3A;
  --color-dark-slate: #4A4A4A;
  --color-warm-gray: #6B6B6B;
  --color-coral: #E88B7A;
  --color-off-white: #FAFAF8;
}

html {
  scroll-behavior: smooth;
}

body {
  min-height: 100vh;
}

/* Skip to content link for accessibility */
.skip-to-content {
  @apply absolute -top-10 left-4 z-50 bg-teal text-white px-4 py-2 rounded-pill transition-all;
}

.skip-to-content:focus {
  @apply top-4;
}
```

- [ ] **Step 4: Update next.config.js**

Replace the entire contents of `next.config.js` (or `next.config.mjs` if that's what was generated):

```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    formats: ["image/webp"],
  },
};

module.exports = nextConfig;
```

- [ ] **Step 5: Verify dev server still starts**

```bash
npm run dev
```

Expected: No errors, page loads with off-white background and Montserrat body font.

- [ ] **Step 6: Commit**

```bash
git add tailwind.config.ts app/layout.tsx app/globals.css next.config.js
git commit -m "feat: configure brand tokens, fonts, and global styles"
```

---

### Task 3: Create Content Constants

**Files:**
- Create: `lib/constants.ts`

- [ ] **Step 1: Create lib/constants.ts**

```ts
export const PHONE_NUMBER = "(540) 740-8937";
export const PHONE_HREF = "tel:+15407408937";

export const heroContent = {
  headline: "A Dream Smile You Never Have to",
  headlineAccent: "Hide",
  bullets: [
    "A doctor who truly cares \u2014 gives you his personal cell number",
    "30 years of dental implant service and experience",
    "No 3rd-parties. A one-stop shop. Everything in-house.",
  ],
  cta: "Schedule Your Free Consultation",
};

export const quizOptions = [
  "I have all of my teeth",
  "I\u2019m missing one tooth",
  "I\u2019m missing multiple teeth",
  "I\u2019m missing all of my teeth",
];

export const quizContent = {
  label: "FIND OUT IF YOU\u2019RE A",
  title: "CANDIDATE FOR DENTAL IMPLANTS",
  subtitle: "Take this 30-second quiz",
  question: "What best describes your current condition?",
  nextButton: "Next \u2192",
};

export const guideContent = {
  heading: "WANT TO LEARN MORE ABOUT DENTAL IMPLANTS?",
  subheading: "Download the FREE Pricing & Information Guide",
  cta: "GET THE FREE GUIDE",
  bookTitle: "The Complete Handbook on Dental Implants",
};

export const navLinks = [
  { label: "Services", href: "#services" },
  { label: "About", href: "#about" },
  { label: "Contact", href: "#contact" },
];

export const footerContent = {
  address: "Jeffrey S. Burns, DDS",
  tagline: "A Dream Smile You Never Have to Hide",
  copyright: `\u00A9 ${new Date().getFullYear()} Jeffrey S. Burns, DDS. All rights reserved.`,
};
```

- [ ] **Step 2: Commit**

```bash
git add lib/constants.ts
git commit -m "feat: add content constants for Phase 1 sections"
```

---

### Task 4: Build Reusable UI Components

**Files:**
- Create: `components/ui/Button.tsx`
- Create: `components/ui/SectionLabel.tsx`
- Create: `components/ui/CheckItem.tsx`

- [ ] **Step 1: Create components/ui/Button.tsx**

```tsx
import Link from "next/link";

type ButtonProps = {
  children: React.ReactNode;
  variant?: "solid" | "outlined";
  href?: string;
  onClick?: () => void;
  className?: string;
  ariaLabel?: string;
};

export default function Button({
  children,
  variant = "solid",
  href,
  onClick,
  className = "",
  ariaLabel,
}: ButtonProps) {
  const base =
    "inline-block font-body font-semibold uppercase tracking-wider text-sm px-8 py-3 rounded-pill transition-colors duration-200 text-center";
  const variants = {
    solid: "bg-teal text-white hover:bg-teal-dark",
    outlined:
      "border-2 border-teal text-teal hover:bg-teal hover:text-white",
  };
  const classes = `${base} ${variants[variant]} ${className}`;

  if (href) {
    return (
      <Link href={href} className={classes} aria-label={ariaLabel}>
        {children}
      </Link>
    );
  }

  return (
    <button onClick={onClick} className={classes} aria-label={ariaLabel}>
      {children}
    </button>
  );
}
```

- [ ] **Step 2: Create components/ui/SectionLabel.tsx**

```tsx
type SectionLabelProps = {
  children: React.ReactNode;
  className?: string;
};

export default function SectionLabel({ children, className = "" }: SectionLabelProps) {
  return (
    <span
      className={`inline-block font-body text-xs font-semibold uppercase tracking-widest text-teal ${className}`}
    >
      {children}
    </span>
  );
}
```

- [ ] **Step 3: Create components/ui/CheckItem.tsx**

```tsx
type CheckItemProps = {
  children: React.ReactNode;
  className?: string;
};

export default function CheckItem({ children, className = "" }: CheckItemProps) {
  return (
    <div className={`flex items-start gap-3 ${className}`}>
      <div className="mt-1 flex-shrink-0 w-5 h-5 rounded-full bg-teal flex items-center justify-center">
        <svg
          className="w-3 h-3 text-white"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
          strokeWidth={3}
        >
          <path strokeLinecap="round" strokeLinejoin="round" d="M5 13l4 4L19 7" />
        </svg>
      </div>
      <span className="text-base leading-relaxed">{children}</span>
    </div>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add components/ui/Button.tsx components/ui/SectionLabel.tsx components/ui/CheckItem.tsx
git commit -m "feat: add reusable UI components — Button, SectionLabel, CheckItem"
```

---

### Task 5: Build Navbar

**Files:**
- Create: `components/Navbar.tsx`

- [ ] **Step 1: Create components/Navbar.tsx**

```tsx
"use client";

import { useState, useEffect } from "react";
import Link from "next/link";
import { motion } from "framer-motion";
import Button from "@/components/ui/Button";
import { PHONE_NUMBER, PHONE_HREF } from "@/lib/constants";

export default function Navbar() {
  const [scrolled, setScrolled] = useState(false);
  const [mobileOpen, setMobileOpen] = useState(false);

  useEffect(() => {
    const onScroll = () => setScrolled(window.scrollY > 50);
    window.addEventListener("scroll", onScroll, { passive: true });
    return () => window.removeEventListener("scroll", onScroll);
  }, []);

  return (
    <nav
      className={`fixed top-0 left-0 right-0 z-50 transition-all duration-300 ${
        scrolled
          ? "bg-white shadow-md"
          : "bg-transparent"
      }`}
    >
      <div className="max-w-content mx-auto px-6 flex items-center justify-between h-20">
        {/* Logo */}
        <Link href="/" className="flex flex-col leading-tight">
          <span
            className={`font-display text-2xl font-bold tracking-wide transition-colors ${
              scrolled ? "text-charcoal" : "text-white"
            }`}
          >
            j burns
          </span>
          <span
            className={`font-body text-[10px] uppercase tracking-widest transition-colors ${
              scrolled ? "text-warm-gray" : "text-white/70"
            }`}
          >
            Jeffrey S. Burns D.D.S.
          </span>
        </Link>

        {/* Desktop CTA */}
        <div className="hidden md:block">
          <Button variant="outlined" href={PHONE_HREF} className={`${
            scrolled
              ? ""
              : "border-white text-white hover:bg-white hover:text-charcoal"
          }`}>
            Call Us {PHONE_NUMBER}
          </Button>
        </div>

        {/* Mobile hamburger */}
        <button
          className="md:hidden flex flex-col gap-1.5 p-2"
          onClick={() => setMobileOpen(!mobileOpen)}
          aria-label="Toggle navigation menu"
        >
          {[0, 1, 2].map((i) => (
            <span
              key={i}
              className={`block w-6 h-0.5 transition-colors ${
                scrolled ? "bg-charcoal" : "bg-white"
              }`}
            />
          ))}
        </button>
      </div>

      {/* Mobile menu */}
      {mobileOpen && (
        <motion.div
          initial={{ opacity: 0, y: -10 }}
          animate={{ opacity: 1, y: 0 }}
          className="md:hidden bg-white shadow-lg px-6 py-4"
        >
          <Button variant="solid" href={PHONE_HREF} className="w-full">
            Call Us {PHONE_NUMBER}
          </Button>
        </motion.div>
      )}
    </nav>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/Navbar.tsx
git commit -m "feat: add sticky Navbar with scroll transparency and mobile menu"
```

---

### Task 6: Build HeroSection

**Files:**
- Create: `components/sections/HeroSection.tsx`

- [ ] **Step 1: Create components/sections/HeroSection.tsx**

```tsx
"use client";

import { motion } from "framer-motion";
import Button from "@/components/ui/Button";
import CheckItem from "@/components/ui/CheckItem";
import { heroContent } from "@/lib/constants";

export default function HeroSection() {
  return (
    <section className="relative min-h-[90vh] flex items-center overflow-hidden">
      {/* Background placeholder — swap for <Image> when asset ready */}
      <div className="absolute inset-0 bg-gradient-to-br from-charcoal via-charcoal/95 to-charcoal/80" />

      {/* Content */}
      <div className="relative z-10 max-w-content mx-auto px-6 py-24 md:py-32 w-full">
        <div className="max-w-2xl">
          <motion.h1
            initial={{ opacity: 0, x: -30 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.6 }}
            className="font-display text-4xl md:text-5xl lg:text-6xl text-white leading-tight mb-8"
          >
            {heroContent.headline}{" "}
            <em className="italic">{heroContent.headlineAccent}</em>
          </motion.h1>

          <motion.div
            initial={{ opacity: 0, x: -30 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.6, delay: 0.2 }}
            className="space-y-4 mb-10"
          >
            {heroContent.bullets.map((bullet, i) => (
              <CheckItem key={i} className="text-white/90">
                {bullet}
              </CheckItem>
            ))}
          </motion.div>

          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5, delay: 0.4 }}
          >
            <Button href="#quiz">{heroContent.cta}</Button>
          </motion.div>
        </div>
      </div>

      {/* DreamSmile watermark — bottom right */}
      <div className="absolute bottom-8 right-8 z-10">
        <span className="font-script text-3xl md:text-5xl text-white/10 select-none">
          DreamSmile
        </span>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/sections/HeroSection.tsx
git commit -m "feat: add HeroSection with animated text and CTA"
```

---

### Task 7: Build QuizAndGuideSection

**Files:**
- Create: `components/sections/QuizAndGuideSection.tsx`

- [ ] **Step 1: Create components/sections/QuizAndGuideSection.tsx**

```tsx
"use client";

import { useState } from "react";
import { motion } from "framer-motion";
import Button from "@/components/ui/Button";
import { quizOptions, quizContent, guideContent } from "@/lib/constants";

export default function QuizAndGuideSection() {
  const [selected, setSelected] = useState<number | null>(null);

  return (
    <section id="quiz" className="bg-[#F5F5F3] py-20 md:py-24">
      <div className="max-w-content mx-auto px-6">
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8 items-start">
          {/* Quiz Card */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5 }}
            className="bg-white rounded-xl p-8 shadow-sm"
          >
            <span className="font-body text-xs font-semibold uppercase tracking-widest text-warm-gray">
              {quizContent.label}
            </span>
            <h3 className="font-body text-lg font-bold text-charcoal mt-1 mb-2">
              {quizContent.title}
            </h3>
            <p className="text-sm text-warm-gray mb-4">{quizContent.subtitle}</p>
            <p className="font-body font-semibold text-charcoal mb-4">
              {quizContent.question}
            </p>

            <fieldset className="space-y-3">
              <legend className="sr-only">{quizContent.question}</legend>
              {quizOptions.map((option, i) => (
                <label
                  key={i}
                  className={`flex items-center gap-3 p-3 rounded-lg border cursor-pointer transition-colors ${
                    selected === i
                      ? "border-teal bg-teal/5"
                      : "border-gray-200 hover:border-teal/50"
                  }`}
                >
                  <input
                    type="radio"
                    name="quiz"
                    value={option}
                    checked={selected === i}
                    onChange={() => setSelected(i)}
                    className="accent-teal w-4 h-4"
                  />
                  <span className="text-sm text-dark-slate">{option}</span>
                </label>
              ))}
            </fieldset>

            <div className="flex justify-end mt-6">
              <Button variant="solid" className="text-xs px-6 py-2">
                {quizContent.nextButton}
              </Button>
            </div>
          </motion.div>

          {/* Book Cover Placeholder */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5, delay: 0.15 }}
            className="hidden lg:flex justify-center"
          >
            <div
              className="bg-slate-200 rounded-lg flex items-center justify-center text-slate-400 text-sm shadow-lg"
              style={{ width: 260, height: 370 }}
            >
              {guideContent.bookTitle}
            </div>
          </motion.div>

          {/* Guide CTA Card */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5, delay: 0.3 }}
            className="bg-white rounded-xl p-8 shadow-sm flex flex-col justify-center text-center"
          >
            <h3 className="font-body text-sm font-bold uppercase tracking-wider text-charcoal mb-3">
              {guideContent.heading}
            </h3>
            <p className="text-sm text-warm-gray mb-6">{guideContent.subheading}</p>
            <Button variant="outlined">{guideContent.cta}</Button>
          </motion.div>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/sections/QuizAndGuideSection.tsx
git commit -m "feat: add QuizAndGuideSection with interactive quiz and guide CTA"
```

---

### Task 8: Build Footer

**Files:**
- Create: `components/Footer.tsx`

- [ ] **Step 1: Create components/Footer.tsx**

```tsx
import Link from "next/link";
import { PHONE_NUMBER, PHONE_HREF, footerContent } from "@/lib/constants";

export default function Footer() {
  return (
    <footer id="contact" className="bg-charcoal text-white py-16">
      <div className="max-w-content mx-auto px-6">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-12">
          {/* Brand */}
          <div>
            <Link href="/" className="inline-block mb-4">
              <span className="font-display text-2xl font-bold">j burns</span>
              <span className="block font-body text-[10px] uppercase tracking-widest text-white/60">
                Jeffrey S. Burns D.D.S.
              </span>
            </Link>
            <p className="font-script text-xl text-teal-light mt-2">
              DreamSmile
            </p>
            <p className="text-sm text-white/50 mt-2">
              {footerContent.tagline}
            </p>
          </div>

          {/* Contact */}
          <div>
            <h4 className="font-body text-xs font-semibold uppercase tracking-widest text-teal mb-4">
              Contact
            </h4>
            <div className="space-y-2 text-sm text-white/70">
              <p>{footerContent.address}</p>
              <p>
                <Link href={PHONE_HREF} className="hover:text-teal transition-colors">
                  {PHONE_NUMBER}
                </Link>
              </p>
            </div>
          </div>

          {/* Quick Links */}
          <div>
            <h4 className="font-body text-xs font-semibold uppercase tracking-widest text-teal mb-4">
              Quick Links
            </h4>
            <div className="space-y-2 text-sm text-white/70">
              <p>
                <Link href="#quiz" className="hover:text-teal transition-colors">
                  Am I a Candidate?
                </Link>
              </p>
              <p>
                <Link href="#services" className="hover:text-teal transition-colors">
                  Services
                </Link>
              </p>
              <p>
                <Link href="#about" className="hover:text-teal transition-colors">
                  About Dr. Burns
                </Link>
              </p>
            </div>
          </div>
        </div>

        <div className="border-t border-white/10 mt-12 pt-8 text-center text-xs text-white/40">
          {footerContent.copyright}
        </div>
      </div>
    </footer>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/Footer.tsx
git commit -m "feat: add Footer with contact info and quick links"
```

---

### Task 9: Assemble Landing Page

**Files:**
- Modify: `app/page.tsx`

- [ ] **Step 1: Replace app/page.tsx with landing page assembly**

```tsx
import Navbar from "@/components/Navbar";
import HeroSection from "@/components/sections/HeroSection";
import QuizAndGuideSection from "@/components/sections/QuizAndGuideSection";
import Footer from "@/components/Footer";

export default function Home() {
  return (
    <>
      <a href="#main" className="skip-to-content">
        Skip to content
      </a>
      <Navbar />
      <main id="main">
        <HeroSection />
        <QuizAndGuideSection />
      </main>
      <Footer />
    </>
  );
}
```

- [ ] **Step 2: Delete any default page styles or components from scaffolding**

Remove the default `app/page.module.css` if it exists, and the default Next.js SVG files from `public/` that aren't needed (like `next.svg`, `vercel.svg`).

- [ ] **Step 3: Verify everything renders**

```bash
npm run dev
```

Open `http://localhost:3000` and confirm:
- Navbar is visible at top, transparent over hero
- Scrolling makes navbar white with shadow
- Hero section shows headline, checkmarks, and CTA button
- Quiz section has working radio buttons
- Footer renders at the bottom
- Mobile hamburger menu works at narrow widths
- No console errors

- [ ] **Step 4: Run build to check for type errors**

```bash
npm run build
```

Expected: Build succeeds with no errors.

- [ ] **Step 5: Commit**

```bash
git add app/page.tsx
git rm app/page.module.css 2>/dev/null; true
git rm public/next.svg public/vercel.svg 2>/dev/null; true
git add -A
git commit -m "feat: assemble Phase 1 landing page — Navbar, Hero, Quiz, Footer"
```
